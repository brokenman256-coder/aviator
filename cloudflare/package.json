{
  "name": "aviator-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "Aviator crash game — Cloudflare Workers + Durable Objects + D1 build.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute aviator-db --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute aviator-db --remote --file=./migrations/0001_init.sql"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
